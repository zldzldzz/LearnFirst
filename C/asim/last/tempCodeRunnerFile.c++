while (fgets(line, sizeof(line), file) != NULL ) {
		printf("%s", line);
	}
		printf("\n\n");
		printf("reg table\n");


    while (fgets(line, sizeof(line), file1) != NULL ) {
		printf("%s", line);
	}
		printf("\n");